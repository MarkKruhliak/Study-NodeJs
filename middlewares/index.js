module.exports = {
    authMDLW : require('./auth.middlewares'),
    commonMDWL: require('./common.middlewares'),
    userMDWL: require('./user.middlewares'),
    fileMDWL: require('./file.middlewares')
}
